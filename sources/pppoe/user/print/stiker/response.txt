<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>Cetak Stiker ONT PPPoE</title>
    <style>
        @page { margin: 8mm; }
        body { font-family: Arial, sans-serif; color: #0b1b2b; margin: 0; background: #eef2f6; }
        .page { padding: 8mm; }
        .page-header { display: flex; align-items: baseline; justify-content: space-between; margin-bottom: 5mm; }
        h1 { font-size: 15px; margin: 0; letter-spacing: 0.2px; }
        .meta { font-size: 10px; color: #52616b; }
        .alert { padding: 10px 12px; background: #fff3cd; color: #664d03; border: 1px solid #ffeeba; }

        .stickers { display: flex; flex-wrap: wrap; gap: 6mm; }
        .sticker {
            width: 120mm;
            height: 82mm;
            background: #fff;
            border: 1px solid #d8e1ea;
            border-radius: 6px;
            box-shadow: 0 2px 0 rgba(0,0,0,0.03);
            padding: 5mm 7mm 5mm;
            box-sizing: border-box;
            position: relative;
            overflow: hidden;
        }
        .top-bar {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6mm;
            background: linear-gradient(90deg, #1f5f7a 0%, #0e8aa7 60%, #2a4d6d 60%, #2a4d6d 80%, #ffd21f 80%, #ffd21f 100%);
        }
        .top-accent {
            position: absolute;
            top: 0;
            right: 16mm;
            width: 18mm;
            height: 6mm;
            background: #e7eef5;
            transform: skewX(-18deg);
        }
        .header-row { display: flex; align-items: center; justify-content: space-between; margin-top: 4mm; }
        .brand-left { display: flex; align-items: center; gap: 6mm; }
        .brand-logo img { max-height: 16mm; max-width: 36mm; object-fit: contain; }
        .brand-logo-text { font-weight: 700; color: #1e6d8c; font-size: 14px; }
        .brand-slogan { font-weight: 600; color: #7a8a97; font-size: 8px; letter-spacing: 0.4px; margin-top: 2px; text-transform: uppercase; }
        .brand-right { font-weight: 700; color: #2a5c7a; font-size: 11px; text-align: right; }

        .identity { display: grid; grid-template-columns: 1fr 1fr; gap: 8mm; margin: 4mm 0 5.5mm; font-size: 11px; }
        .id-row { display: flex; align-items: center; gap: 3mm; }
        .id-row span { width: 16mm; font-weight: 700; color: #1c3f59; }
        .id-line { flex: 1; border-bottom: 1px solid #1c3f59; height: 15px; align-items: flex-end; }
        .id-line span { padding-bottom: 1px; font-weight: 600; color: #0b1b2b; }

        .title { text-align: center; font-weight: 700; color: #2b4d68; font-size: 10px; margin-bottom: 2.5mm; }
        .steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2.5mm; margin-bottom: 2mm; }
        .step {
            background: #2c4f6f;
            color: #fff;
            border-radius: 10px;
            padding: 16px 7px 9px;
            position: relative;
            font-size: 8.5px;
            line-height: 1.25;
            min-height: 17mm;
        }
        .step::before {
            content: attr(data-no);
            position: absolute;
            top: -6px;
            left: 6px;
            background: #1b7cb0;
            width: 16px;
            height: 16px;
            border-radius: 4px;
            display: grid;
            place-items: center;
            font-size: 9px;
            font-weight: 700;
        }
        .step-contact { display: block; margin-top: 4px; font-weight: 600; }

        .mid-row { display: grid; grid-template-columns: 2fr 1fr; gap: 4mm; align-items: center; margin-bottom: 2mm; }
        .notice {
            background: #3f7fbf;
            color: #fff;
            border-radius: 6px;
            padding: 6px 8px;
            font-size: 9px;
            line-height: 1.3;
        }
        .qr-wrap { display: grid; grid-template-columns: 12mm 1fr; gap: 3mm; align-items: center; }
        .qr-box { height: 9mm; width: 12mm; background: #fff; display: grid; place-items: center; }
        .qr-box img { width: 100%; height: 100%; object-fit: cover; }
        .qr-text { font-size: 8px; font-weight: 700; color: #2b4d68; text-transform: uppercase; }

        .bottom-row { display: grid; grid-template-columns: 1fr 1.2fr 1.4fr; gap: 3mm; font-size: 8.5px; }
        .info { background: #8bc34a; color: #134200; border-radius: 4px; padding: 5px 6px; font-weight: 700; text-transform: uppercase; }
        .ownership { background: #234664; color: #fff; border-radius: 4px; padding: 5px 6px; font-weight: 700; text-align: center; text-transform: uppercase; }
        .warning { background: #e53935; color: #fff; border-radius: 4px; padding: 5px 6px; font-weight: 700; display: flex; align-items: center; gap: 6px; }
        .warning::before { content: "!"; background: #fff; color: #e53935; width: 16px; height: 16px; border-radius: 50%; display: grid; place-items: center; font-weight: 900; }

        @media print {
            body { background: #fff; }
            .page { padding: 0; }
            .page-header { margin-bottom: 4mm; }
        }
    </style>
</head>
<body>
        <div class="page">
        <div class="page-header">
            <h1>Stiker ONT PPPoE</h1>
            <div class="meta">Dicetak: 2026-05-17 16:51 | Total: 0</div>
        </div>

                    <div class="alert">Pilih user dulu (centang checkbox).</div>
            </div>

    </body>
</html>
